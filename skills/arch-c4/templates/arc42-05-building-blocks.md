---
title: "{{project_name}} — Building Block View (arc42 §5)"
status: draft
owner: {{owner}}
last_reviewed: {{today}}
review_interval: 180d
---

# 5. Building Block View

> "The building block view shows the **static** decomposition of the system into building blocks (modules, components, subsystems, classes, interfaces, packages, libraries, frameworks, layers, partitions, tiers, functions, macros, operations, data structures, …) as well as their dependencies. This view is mandatory for every architecture documentation. In analogy to a house this is the *floor plan*." — arc42 §5

The Building Block View is **structural**. Behaviour and time-ordered scenarios belong to [§6 Runtime View](./06-runtime-view.md). Aggregate invariants, lifecycle state machines, and command-event listings belong to [`docs/domain/07b-models/`](../../domain/07b-models/) — this section names containers and components and **links back** to the domain model, never re-states it.

## 5.1 Whitebox Overall System (Level 1)

### Motivation for the decomposition

_(Narrative — owned by `arch-arc42`; outside the generated block below.)_ _TODO_ — one paragraph explaining the top-level decomposition rationale (e.g. "Microservices per bounded context to allow independent scaling and team ownership", or "Modular monolith because the domain is too small to justify deployment overhead").

### Contained Building Blocks

<!-- arch-c4:start key=containers — generated by `arch-c4 container`; DO NOT hand-edit between these markers (the diagram + table are a projection of workspace.dsl). All prose OUTSIDE the markers is owned by arch-arc42. See ADR-0004. -->
![{{system_id}} Containers](../c4/views/containers.svg)

| ID | Name | Technology | Responsibility | Domain aggregates implemented |
|---|---|---|---|---|
| _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |
<!-- arch-c4:end key=containers -->

(One row per `CON-NN` defined in `workspace.dsl`. The `Domain aggregates implemented` column is **mandatory** — it is the back-reference from this Building Block View into [`docs/domain/07b-models/`](../../domain/07b-models/). Components implementing the same aggregate may share the row's BC-NN.AGG-NN value. Empty cells are permitted for containers that genuinely implement no domain logic, e.g. an event bus or CDN.)

### Important Interfaces

_(Narrative — owned by `arch-arc42`.)_ _TODO_ — list `CTR-NN` interface contracts from [`docs/architecture/interfaces/`](../interfaces/) that are critical to understanding the decomposition. Skip if all inter-container communication is plain HTTPS/JSON.

## 5.2 Level 2 — Selected Building Blocks

> "Please prefer relevance over completeness. Specify important, surprising, risky, complex or volatile building blocks. Leave out normal, simple, boring or standardised parts of your system." — arc42 §5.2

One sub-subsection per drilled container. Each is generated by running `arch-c4 component <CON-NN>`. Routine CRUD services and off-the-shelf containers are intentionally not drilled.

Each drilled §5.2.x carries its own generated block — the component diagram + components table — fenced by `<!-- arch-c4:start key=components-CON-NN -->` … `<!-- arch-c4:end key=components-CON-NN -->`. Any narrative around it (why this container was drilled) is owned by `arch-arc42`, outside the markers.

<!-- §5.2.x sub-subsections appended below by `arch-c4 component` runs -->

_No containers drilled yet. Run `arch-c4 component <CON-NN>` for the most architecturally-relevant container first._

## 5.3 Level 3 — Deeper drills (if needed)

Used sparingly when a Component itself has internal substructure worth surfacing. Most projects never write this section.

## Cross-references

| Linked artefact | Relationship |
|---|---|
| [`docs/domain/07b-models/`](../../domain/07b-models/) | Domain aggregates (`BC-NN.AGG-NN`) implemented by Components in §5.2 |
| [`docs/architecture/decisions/`](../decisions/) | ADRs governing technology choices visible in §5.1 |
| [`docs/architecture/interfaces/`](../interfaces/) | Service contracts (`CTR-NN`) defining the wire formats between containers |
| [`docs/architecture/arc42/03-context.md`](./03-context.md) | What's *outside* the system — counterpart to this section |
| [`docs/architecture/arc42/06-runtime-view.md`](./06-runtime-view.md) | How the building blocks here interact over time |
| [`docs/architecture/arc42/07-deployment.md`](./07-deployment.md) | How these building blocks map onto infrastructure |
| [`docs/product-specs/09a-quality-attributes.md`](../../product-specs/09a-quality-attributes.md) | Quality requirements (`QA-XXNN`) attached to specific containers / components |

## Open Items

| OI-ID | Type | Summary | Source anchor | Source heading | Resolution path | Priority | Status | Owner | Due / Review date | Tracker ref |
| :---- | :--- | :------ | :------------ | :------------- | :-------------- | :------- | :----- | :---- | :---------------- | :---------- |

_None at present._
