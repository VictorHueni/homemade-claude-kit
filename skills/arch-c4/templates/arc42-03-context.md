---
title: "{{project_name}} — Context and Scope (arc42 §3)"
status: draft
owner: {{owner}}
last_reviewed: {{today}}
review_interval: 180d
---

# 3. Context and Scope

> "Context and scope delimits your system from all its communication partners (neighbouring systems and users). It thereby specifies the external interfaces." — arc42 §3

This section documents the **black-box** view of {{project_name}}: who and what it talks to. Internal containers and components belong to [§5 Building Block View](./05-building-blocks.md).

## 3.1 Business Context

The diagram below shows {{project_name}} ({{system_id}}) and every communication partner. Black-box treatment — internals are hidden.

### Communication partners

<!-- arch-c4:start key=systemContext — generated by `arch-c4 context`; DO NOT hand-edit between these markers (diagram + table project workspace.dsl). Prose OUTSIDE the markers is owned by arch-arc42. See ADR-0004. -->
![{{system_id}} System Context](../c4/views/systemContext.svg)

| Partner | Inputs (from us) | Outputs (to us) |
|---|---|---|
| _TODO_ | _TODO_ | _TODO_ |
<!-- arch-c4:end key=systemContext -->

(One row per `P-NN` actor or external `SYS-NN`. Fill from the relationships defined in `workspace.dsl`.)

### Notable domain interactions

_(Narrative — owned by `arch-arc42`.)_ _TODO_ — describe any domain-specific protocols, message envelopes, or content models that are part of the agreement between {{system_id}} and a partner. Skip if all interactions are plain HTTPS/JSON CRUD.

## 3.2 Technical Context

The technical channels and protocols between {{system_id}} and its partners. Derived from the `technology` field on each Structurizr DSL relationship.

<!-- arch-c4:start key=systemContext-technical — generated by `arch-c4 context`; DO NOT hand-edit between these markers. -->
| Partner | Channel | Protocol | Format | Notes |
|---|---|---|---|---|
| _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |
<!-- arch-c4:end key=systemContext-technical -->

### Mapping input/output to channels

_(Narrative — owned by `arch-arc42`.)_ _TODO_ — for each row above, briefly note which business inputs/outputs (from §3.1) flow over which channel. Skip if the mapping is 1:1 obvious.

## Cross-references

| Linked artefact | Relationship |
|---|---|
| [`docs/business/01a-personas.md`](../../business/01a-personas.md) | Persona definitions (`P-NN`) reused as actors here |
| [`docs/domain/02b-bounded-contexts.md`](../../domain/02b-bounded-contexts.md) | External SaaS in the BC map's `Generic` subdomains often appear here as external systems |
| [`docs/architecture/interfaces/`](../interfaces/) | Service contracts (`CTR-NN`) define the wire formats for partners |
| [`docs/architecture/arc42/05-building-blocks.md`](./05-building-blocks.md) | What's *inside* the black box drawn above |

## Open Items

| OI-ID | Type | Summary | Source anchor | Source heading | Resolution path | Priority | Status | Owner | Due / Review date | Tracker ref |
| :---- | :--- | :------ | :------------ | :------------- | :-------------- | :------- | :----- | :---- | :---------------- | :---------- |

_None at present._

(Schema follows the kit's Open Items Governance contract: every row is one of `doc-gap` / `decision-gap` / `execution-item` / `tech-debt`. Sync to [`docs/project-control/open-items/`](../../project-control/open-items/) via the `util-open-items` skill.)
