# CLAUDE.md / AGENTS.md — Template

Copy this file to `CLAUDE.md` (Claude Code) or `AGENTS.md` (cross-agent) at the project root. Trim every section marked `<!-- optional -->` if it does not apply. The goal is the shortest file that prevents the most repeated mistakes.

**Line budget:** target < 200 lines total. If you exceed that, move the overflow to `agent_docs/` reference files and link from here.

---

```markdown
# CLAUDE.md

<!-- ================================================================
  BEHAVIORAL POSTURE — keep this block verbatim. It applies to every
  task regardless of stack, language, or project type.
================================================================ -->
## Behavioral guidelines

**Think before coding.** State assumptions explicitly. If multiple interpretations exist, present them — don't pick silently. If something is unclear, stop and ask.

**Simplicity first.** Minimum code that solves the problem. No speculative features, no abstractions for single-use code, no unrequested flexibility or error handling for impossible scenarios. If you write 200 lines and it could be 50, rewrite it. Ask: "would a senior engineer call this overcomplicated?" If yes, simplify.

**Surgical changes.** Touch only what you must. Don't improve adjacent code, comments, or formatting. Don't refactor things that aren't broken. Match existing style. If you notice unrelated dead code, mention it — don't delete it. Every changed line should trace directly to the request.

**Goal-driven execution.** Transform tasks into verifiable success criteria and loop until verified. "Add validation" → "Write tests for invalid inputs, then make them pass." "Fix the bug" → "Write a test that reproduces it, then make it pass." For multi-step work, state a brief plan with a verification step per increment before starting.

These guidelines are working if: fewer unnecessary changes in diffs, clarifying questions before implementation rather than after mistakes.

---

<!-- ================================================================
  WHAT — stack identity. One line per dependency, no explanations.
================================================================ -->
## Project

<!-- One sentence: what this product does and for whom. -->
_TODO: <PRODUCT_NAME> is a <type> that helps <audience> <do what>._

**Stack:** _TODO: <Framework> <version> · <Runtime> <version> · <DB/auth> · <key deps>_
**Package manager:** _TODO: <pnpm / npm / bun / pip / cargo / …>_

<!-- ================================================================
  WHY — docs navigation. Point to the living index, not inline paths.
================================================================ -->
## Documentation

<!-- If docs/INDEX.md exists (generated by util-metamodel-scaffold or manually): -->
- Product vision: [docs/VISION.md](docs/VISION.md)
- Full docs navigation: [docs/INDEX.md](docs/INDEX.md)

<!-- If no INDEX.md yet, link what exists: -->
<!-- - Product vision: [docs/VISION.md](docs/VISION.md) -->
<!-- - Architecture decisions: [docs/architecture/decisions/](docs/architecture/decisions/) -->

---

<!-- ================================================================
  HOW — exact, runnable commands. Use CI as the canonical source.
================================================================ -->
## Commands

```bash
# Install
_TODO: <install command>

# Dev server
_TODO: <dev command>

# Type-check / lint
_TODO: <typecheck command>
_TODO: <lint command>

# Tests (fast — run before every commit)
_TODO: <test:unit command>

# Tests (full suite — run before PR)
_TODO: <test:full command>

# Build
_TODO: <build command>
```

---

<!-- ================================================================
  PERMISSION BOUNDARY — what the agent may do without asking.
================================================================ -->
## What the agent may do autonomously

| Allowed without asking | Requires human approval |
|---|---|
| Read any file | `git push` / open PRs |
| Run linter / formatter | Install or remove packages |
| Run unit tests on a specific file | Run the full test suite |
| Create or edit files in `src/` | Delete files |
| _TODO: add project-specific rows_ | Run migrations / `terraform apply` |

---

<!-- optional =====================================================
  DOMAIN CONTEXT — only terms non-obvious to a competent engineer
  unfamiliar with this codebase. Link to glossary if one exists.
================================================================ -->
## Domain context

<!-- Add only if terms would surprise a newcomer. Otherwise delete this section. -->
<!-- Link: [domain glossary](docs/domain/02c-glossary.md) -->

- **_TODO: <term>_**: _one-sentence definition specific to this codebase_

---

<!-- optional =====================================================
  SECRETS — where they live, never their values.
================================================================ -->
## Secrets

Never write credentials, tokens, or connection string values into any file.

| Secret | Lives in |
|---|---|
| _TODO: `<VAR_NAME>`_ | _TODO: `.env.local` (local) · GitHub Actions secret (CI)_ |

See `.env.example` for the full list of required variable names.

---

<!-- optional =====================================================
  REFERENCE DOCS — task-specific workflows too large for this file.
================================================================ -->
## Reference docs

<!-- Uncomment and populate as agent_docs/ files are created: -->
<!-- - Build and deploy: [agent_docs/building.md](agent_docs/building.md) -->
<!-- - DB migrations: [agent_docs/migrations.md](agent_docs/migrations.md) -->
<!-- - Testing patterns: [agent_docs/testing.md](agent_docs/testing.md) -->
```

---

## Sections reference

| Section | Required | Notes |
|---|---|---|
| Behavioral guidelines | Yes | Copy verbatim; never trim |
| Project (WHAT) | Yes | One-liner + stack table |
| Documentation (WHY) | Yes | Pointer to INDEX.md or VISION.md |
| Commands (HOW) | Yes | Exact commands with flags; fill when stack is stable |
| Permission boundary | Yes | At minimum: git push, installs, file deletion |
| Domain context | Optional | Add when domain terms are non-obvious |
| Secrets | Optional | Add when ≥1 secret is needed to run the project |
| Reference docs | Optional | Add when any section would push file over 200 lines |
