# Trivy suppressions — one check ID per line. Keep this reviewable: every
# suppression should have a comment explaining why it is safe to ignore.
# Example:
# EXO-001  # jump host is intentionally world-reachable on 22; access gated by MFA
